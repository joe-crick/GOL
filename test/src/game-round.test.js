import initGame from 'GameOfLife/src/game-init';

let grid;

describe('Game Round', () => {

	beforeEach(() => {
		grid = initGame({cols: 3, rows: 3});
	});

	describe('Plays a Round', () => {

		it('correctly updates cells based on round', () => {

		});

	});

});
